# pnpm settings for this docs site.
# allowBuilds: explicitly allow postinstall build scripts (pnpm blocks them by
# default for safety). @swc/core powers the @docusaurus/faster pipeline;
# core-js's postinstall is cosmetic (a donation notice / no-op).
allowBuilds:
  '@swc/core': true
  core-js: true
